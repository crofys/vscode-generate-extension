import { createReactHandler } from '../components/default';
import { createTableHandler } from '../components/table';
export const REACT: any = {
  default: createReactHandler,
  table: createTableHandler,
};
