import { IsNotEmpty, MaxLength, IsString } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';

export class Create<%- cameName %>Dto {
  @ApiProperty({ description: '名称' })
  @IsNotEmpty()
  @IsString()
  name: string;
}
